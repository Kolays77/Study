Богачевская домашка
1) аргументы: массив длины n, само число n. Функция складывает в а[i] i-ое простое число

2) Аргументы: имя файла, массив a[n] вещественных чисел, его длина. Складывает в a[i] i-ый по величине элемент последовательность. Возвращает количество заполненных элементов

3) как 9ая из предыдущей, но без буферного массива

4) Аргументы: a[n], n. Удалить все элементы, меньшие среднего геометрического соседних (для тех, для которых можно посчитать это дерьмо). Вернуть кол-во оставшихся элементов. За n +О(1)

5) Аргументы: n, a[n] и b[n], x. Это отрезки с началомв a[i] и концом в b[i]. Вернуть 0 или 1. Есть ли общее пересечение. А в х записать любую общую точку, если есть

6) То же, но только про объединение. Если не отрезок, то 0, если отрезок, то 1
