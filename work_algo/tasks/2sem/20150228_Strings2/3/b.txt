int main(int argc, char *argv[])
	if (argc != 1)
	if (!fgets(str, LENGTH, stdin))ёё
	if (result < 0)
		switch (result)
int make_string(char *str)
	for (idx = 0; str[idx]; idx++)
		if (str[idx] >= 'A' && str[idx] <= 'Z')
		if (str[idx] == '\n')
		  char *str)
	if (!(fp1 = fopen(input, "r")))
	while (fgets(buffer, MAX_LENGTH, fp1))
		if (strstr(buffer, str))
	if (!feof(fp1))
